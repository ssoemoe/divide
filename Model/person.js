module.exports = {
    addPerson: function (req, res, conn, fname, lname, username, email, image, pwd, phNum, position, team, createdDate) {
        
        // conn.query("insert into ", ());
        // res.status(200).sendFile("./View/homePage.html");
    }
};